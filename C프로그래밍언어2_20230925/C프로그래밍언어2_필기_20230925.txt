C프로그래밍언어_필기_20230925

소프트웨어 세계, 변하지 않는 것은 변화뿐!

데이터가 관련성을 가지면
하나의 데이터처럼 취급

-> 데이터가 변경 사항이 생겨도
전체를 변경하지 않아도 괜찮음

=> 지역적인 코드화

void 포인터
형식이 존재하지 않음
주소를 담아도 괜찮음
-> 실제 값에 상관 없이

주소 자체에는 의미가 없음
주소를 보관하는 용도


//

문자열 형식
String

하지만, C언어는 문자를 직접 다룸

'A' -> 문자
"A" -> 문자열 .. 'A' + '\0'
문자들의 연속적인 집합, 문자열

\0, 널문자
코드 값은 0

Char 1Byte

char c = 'A' (o)
char c = '가' (x)

하지만..

char* c = "A" (o)
char* c = "가" (o)
실제 값을 담는 게 아니라 
주소를 담는 것이니 괜찮

문자열은 고정 못 함
문자열의 크기는 달라지기 떄문
그렇다면 문자열을 담기 위해선?
문자열의 시작 주소를 줘야 함
(문자열의 끝에는 널문자가 있음

//

k(p + i) = p[i]

//